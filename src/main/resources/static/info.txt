static
Хранятся статические ресурсы (отдаются пользователю «как есть»).

Это файлы, которые не обрабатываются шаблонизатором.

Обычно тут:

static/
 ├── css/       → стили (.css)
 ├── js/        → скрипты (.js)
 ├── img/       → картинки (jpg, png, svg)
 └── fonts/     → шрифты